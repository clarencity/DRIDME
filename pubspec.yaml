name: dridmebemvp
description: A new Flutter project.
publish_to: 'none' # Prevent accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensure compatibility with Dart SDK

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  intl: ^0.20.2
  firebase_storage: ^11.0.0
  file_picker: ^5.0.0
  image_picker: ^1.1.2
  shared_preferences: ^2.2.2

#
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Use only one version for this dependency

flutter:
  uses-material-design: true
  assets:
    - assets/avatar_placeholder.png